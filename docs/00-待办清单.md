# PUBG 复盘分析台 — 待办清单

> 版本：v0.1  
> 更新日期：2026-08-28（玩家页近况现状卡）  
> 相关文档：`01-PRD-页面线框.md`、`02-API与表结构设计.md`、`05-API提额申请材料.md`  
> 代码目录：项目根目录（Next.js + TS）  
> 说明：未 PUSH 前的改动视为同一版本；本清单用于跟踪落地进度，完成一项勾选一项。

---

## 状态图例

| 标记    | 含义       |
|-------|----------|
| `[ ]` | 未开始      |
| `[~]` | 进行中      |
| `[x]` | 已完成      |
| `[-]` | 已取消 / 暂缓 |

---

## 0. 文档与筹备（当前阶段）

| 状态  | 待办                        | 说明                                      | 优先级 |
|-----|---------------------------|-----------------------------------------|-----|
| [x] | 产品定位确认                    | 复盘分析台；技术栈先不定                            | P0  |
| [x] | 撰写页面线框级 PRD               | `01-PRD-页面线框.md`                        | P0  |
| [x] | 撰写 API / 表结构 / 复盘规则       | `02-API与表结构设计.md`                       | P0  |
| [x] | 整理待办清单                    | 本文 `00-待办清单.md`                         | P0  |
| [x] | 申请 PUBG Developer API Key | https://developer.pubg.com/             | P0  |
| [x] | API Key 连通性验证             | 本地冒烟已通过（search → season → match）        | P0  |
| [x] | 确认技术栈                     | Next.js + TypeScript + Tailwind         | P0  |
| [x] | 初始化代码仓库与目录结构              | `pubg-review/`（App Router + API Routes） | P0  |
| [x] | 补充 `03-复盘规则测试用例.md`       | 标签判定样例与边界（无遥测）                          | P1  |
| [x] | 补充 `04-Telemetry解析说明.md`  | 事件映射、采样、存储格式                            | P1  |

### 0.1 API Key 申请后立刻做

```text
[x] 在 developer.pubg.com 创建 App 并复制 API Key
[x] 本地配置 PUBG_API_KEY（勿提交 Git）
[x] 冒烟验证：search → season → match
[x] 记录默认限额：约 10 RPM，超限返回 429
[x] （可选）准备提额材料：产品说明、缓存策略、Demo 说明 → `05-API提额申请材料.md`（正式提交见 M4）
```

### 0.2 技术栈确认后立刻做

```text
[x] 选定前端 / 后端：Next.js + TypeScript（缓存/DB 后续）
[x] 生成项目骨架：pubg-review
[x] 接入配置项：PUBG_API_KEY（.env.example / .env.local）
[x] 实现 PubgClient 最小封装 + smoke / search / overview / match API
[x] 本地填写 .env.local 并完成冒烟验证
[ ] Redis（后续）；DB 表暂以 `.data/` 本地文件替代
```

---

## 1. 里程碑 M1 — 可用查询站

> 目标：搜索 → 概览 → 对局列表 → 对局详情（积分板）可跑通，并有缓存。

### 1.1 后端

| 状态  | 待办                                                           | 对应能力                                                                         |
|-----|--------------------------------------------------------------|------------------------------------------------------------------------------|
| [x] | PubgClient（429 处理、gzip Accept）                               | 基础设施                                                                         |
| [x] | `GET /api/v1/players/search`                                 | 搜索                                                                           |
| [x] | `GET /api/v1/players/{accountId}/overview`                   | 赛季概览                                                                         |
| [x] | `GET /api/v1/players/dashboard`                              | 玩家页聚合（含近期对局）                                                                 |
| [x] | `GET /api/v1/matches/{matchId}`                              | 对局详情                                                                         |
| [x] | `POST /api/v1/players/{accountId}/refresh`                   | 手动刷新 + 60s 冷却（40901）                                                         |
| [x] | `GET /api/v1/meta/seasons`                                   | 赛季下拉                                                                         |
| [x] | 进程内内存缓存（name/season/match）                                   | 限流保护（Redis 后续）                                                               |
| [x] | 表：player / match / match_participant / season_stats_snapshot | **本地文件替代 DB**：`.data/matches/`、`.data/seasons/`（+ history）；正式 MySQL/Redis 后续 |
| [x] | Match 详情串行补齐（dashboard 内）                                    | 体验                                                                           |

### 1.2 前端

| 状态  | 待办                             | 对应页面          |
|-----|--------------------------------|---------------|
| [x] | 首页搜索（平台 + 昵称 + 历史）             | `/`           |
| [x] | 玩家概览 KPI + 模式筛选                | `/player/...` |
| [x] | 近期对局列表（基础版）                    | 玩家页内嵌         |
| [x] | 对局详情积分板 + 个人数据条                | `/match/...`  |
| [x] | 全局 Loading / 空态 / 404 / 429 文案 | 统一友好文案        |
| [x] | 刷新按钮真实调用 refresh API           | 玩家页           |
| [x] | 赛季下拉（`?seasonId=`）             | 玩家页           |

### 1.3 M1 验收

```text
[x] 四平台至少验证 steam（其余按需）
[x] 缓存命中时概览首屏 < 2s（本地环境可放宽）
[x] 同一 match 不重复打官方详情（getCachedMatch）
[x] API Key 未出现在前端与仓库
[x] 14 天无对局时有明确提示
```

---

## 2. 里程碑 M2 — 复盘报告与分析

> 目标：每场有可解释报告；玩家页有弱点标签与分析 Tab。

### 2.1 后端

| 状态  | 待办                                       | 对应能力                                                     |
|-----|------------------------------------------|----------------------------------------------------------|
| [x] | 复盘规则引擎 v1.0.0（无遥测降级版）                    | `report-engine` + `report-service`                       |
| [x] | `GET /matches/{matchId}/report`          | 单场报告（内存 + `.data/reports/`）                              |
| [x] | `POST /matches/{matchId}/report/rebuild` | 重算（调试；覆盖落盘）                                              |
| [x] | `GET /players/analysis`（name+platform）   | 聚合诊断（近 N 场报告）                                            |
| [x] | 表：match_report                           | **本地文件替代 DB**：`.data/reports/{matchId}/{accountId}.json` |
| [-] | 收藏 API：GET/POST/DELETE favorites         | 本期改用 localStorage device 级                               |
| [-] | 表：user_favorite（可先 device 级）             | 本期本地收藏，服务端表后续                                            |
| [x] | dashboard 聚合 weaknessTags                | 概览增强                                                     |
| [x] | dashboard `trend` 近 14 天按日聚合 + 概览图       | 官方 recent + 本地历史，不额外打官方                                  |

### 2.2 前端

| 状态  | 待办                      | 对应页面                        |
|-----|-------------------------|-----------------------------|
| [x] | 对局详情：复盘报告区块（降级版）        | `/match/...` 首卡             |
| [x] | 玩家近期对局表「标签」列            | primaryTag                  |
| [x] | 玩家概览弱点标签（可点击过滤）         | `?tag=code`                 |
| [x] | 分析 Tab（雷达条 + 问题列表 + 建议） | `?tab=analysis`             |
| [x] | 收藏按钮与收藏页（localStorage）  | `/favorites`                |
| [x] | 分享卡雏形（可选）               | `/share/match/...` + 复制分享链接 |

### 2.3 M2 验收

```text
[x] 每场报告至少 1 个主因标签（或明确 good_game）
[x] 无遥测时仍能出降级报告，并标注「初判」
[x] 弱点标签可跳到对局列表过滤结果
[x] 收藏后可在收藏页看到并进入玩家页（本机；未做批量强制刷新）
```

---

## 3. 里程碑 M3 — Telemetry 与回放

> 目标：事件轴 + 2D 回放 MVP；报告可被遥测增强重算。

### 3.1 后端

| 状态  | 待办                                        | 对应能力                                        |
|-----|-------------------------------------------|---------------------------------------------|
| [x] | 下载并私有存储原始 telemetry                       | `.data/telemetry/` 本地模拟对象存储                 |
| [x] | 表：telemetry_asset                         | meta.json 状态机（DB 表后续）                       |
| [x] | 解析器：轨迹采样 / 击杀 / 倒地 / 圈 / 空投               | TelemetryService                            |
| [x] | `POST /matches/{matchId}/telemetry/parse` | 触发解析                                        |
| [x] | `GET /matches/{matchId}/telemetry/events` | 精简事件流                                       |
| [x] | 遥测就绪后重算 report（提升 confidence）             | `1.1.0-telemetry`：本地 events ready 自动增强；否则降级 |
| [-] | 队列：telemetry.parse / report.build         | 本期同步 inflight 互斥；队列后续                       |

### 3.2 前端

| 状态  | 待办                      | 对应页面     |
|-----|-------------------------|----------|
| [x] | 事件轴 Tab（过滤 + 跳转时间）      | timeline |
| [x] | 2D 回放 MVP（地图、轨迹、时间轴、倍速） | replay   |
| [x] | 图层开关（轨迹/圈/名字/枪线）        | replay   |
| [x] | 解析中 / 过期 / 失败降级态        | UX       |

### 3.3 M3 验收

```text
[x] 浏览器不直接拉取官方原始超大 JSON
[x] 回放拖动时间轴可用，关键操作流畅
[x] 无遥测对局隐藏或禁用回放并说明原因
[x] 增强报告与降级报告可区分（ruleVersion + degraded；telemetry ready 自动增强）
```

---

## 4. 里程碑 M4 — 留存与增长

> 目标：历史同步、武器/地图分析、对比与导出。  
> 本期交付为**务实开发版**：本地 `.data/` 落盘替代定时 Job；不提交正式提额申请。

| 状态  | 待办                      | 说明                                              |
|-----|-------------------------|-------------------------------------------------|
| [-] | 收藏玩家定时同步 Job（打散、限流）     | 暂缓；改用本地历史 + 手动「同步近况」                            |
| [x] | 本地历史库 `.data/history/`  | dashboard upsert；按 accountId JSON               |
| [x] | 同步近况按钮（90s 冷却）          | `POST .../history/sync`，复用限流思路                  |
| [x] | 玩家改名历史（轻量）              | `.data/name-history/`；玩家页提示                     |
| [x] | 武器 Tab 聚合               | `?tab=weapons`；无 telemetry 则 participant 降级     |
| [x] | 地图 Tab 聚合               | `?tab=maps`；胜率/KD≈/场均伤                          |
| [x] | 好友/多玩家对比                | `?tab=compare&vs=`；最多 2 人 KPI 表                 |
| [x] | CSV 导出（积分板）             | 对局详情 scoreboard 客户端下载                           |
| [x] | 车队同场统计（P0）              | `tab=squad`；齐全场聚合；insights；CSV；`.data/squad` 缓存 |
| [-] | 分享海报完善（图片 OG）           | 已有链接分享 + 简单 OG metadata；海报后续                    |
| [x] | 封禁状态（官方 banType）        | 玩家页 BanStatusCard；不做开挂判定                        |
| [x] | 近况现状卡（form + anomalies） | 概览顶；`getPlayerFormAnalysis` 与 dashboard 并行；失败降级 |
| [-] | （可选）武器精通 Mastery        | 增值，后续                                           |
| [x] | 准备提额材料（产品/缓存/请求模式/合规）   | `05-API提额申请材料.md`（中英对照可粘贴）                      |
| [ ] | 申请提高官方 API Rate Limit   | **材料已备，上线/公网 Demo 后提交**；BFF 限流已落地               |

### 4.1 M4 验收

```text
[x] 已同步对局写入本地历史；官方 14 天列表外仍可在「历史库」看到（本机 .data）
[x] 武器/地图 Tab 支持 gameMode 过滤，与概览模式筛选一致
[x] 同步近况 90s 冷却 + 串行拉 match，避免打满 10 RPM
[x] 对比 / CSV 导出可用
[x] 车队 Tab：mates 齐全场统计 + insights + CSV；缓存 `.data/squad/`（refresh=1 绕过）
```

### 4.2 车队同场统计（完成项）

```text
[x] getSquadStats：齐全场 perPlayer / sample / matches
[x] insights：齐全率、贡献偏低、0 吃鸡、平均队排偏高等（客观中文）
[x] GET /api/v1/squad/stats + suggest-mates；玩家页 tab=squad
[x] 客户端导出齐全对局明细 CSV（含人均汇总 section）
[x] `.data/squad/{hash}.json` 落盘（platform+成员 accountId 排序 hash；TTL ~20min；refresh=1）
[x] 缓存 miss 仍串行；磁盘 match 优先
```

---

## 5. 工程与合规（贯穿全程）

| 状态  | 待办                     | 说明                                                           |
|-----|------------------------|--------------------------------------------------------------|
| [x] | `.env` / 密钥管理，禁止提交 Key | `.gitignore` + `.env.example`                                |
| [x] | 网关限流（IP / 粗粒度）         | `src/proxy.ts` 滑动窗口；`RATE_LIMIT_RPM`（默认 30）；42902            |
| [x] | 统一错误码与中文文案             | 404/429/40901/42902 友好文案                                     |
| [x] | 日志与 `api_sync_log`（简易） | `.data/logs/api-sync.jsonl`；开发用 `GET /api/v1/meta/sync-logs` |
| [x] | 标注数据来源：PUBG 官方 API     | 页脚合规标注                                                       |
| [x] | 仅 opt-in 查询，不做全量爬取     | 当前仅主动搜索                                                      |
| [x] | 不对外提供「山寨官方 API」        | ToS                                                          |
| [x] | README：本地启动、配置项、里程碑说明  | 工程                                                           |

---

## 6. 建议执行顺序（当前）

已完成：API Key、技术栈、骨架、冒烟、M1 主流程 + 刷新冷却 + 赛季下拉 + 错误态。

接下来建议：

1. **[x] 收尾 M1**：刷新冷却、赛季下拉、验收勾选、错误态完善
2. **[x] 启动 M2 核心**：无遥测规则引擎 + 对局报告 API/页 + 列表标签列
3. **[x] M2 增强**：分析 Tab、弱点标签聚合、本地收藏
4. **[x] M3**：Telemetry 解析 + 事件轴 / 2D 回放 MVP
5. **[x] M4（务实版）**：本地历史库、同步近况、武器/地图/对比 Tab、CSV、改名提示
6. **[x] 遥测增强规则引擎**（`1.1.0-telemetry`）；**[x] trend / 网关限流 / api_sync_log**；**[x] match/report/season 落盘**；**[x] 提额材料**；**[x] 车队同场统计 P0**；**[x] 玩家页近况现状卡**；**[ ]** Redis；**[ ]** 正式提额（材料已备，上线后提交）

---

## 7. 文档索引

| 文件                    | 内容                   |
|-----------------------|----------------------|
| `00-待办清单.md`          | 本文：进度与执行顺序           |
| `01-PRD-页面线框.md`      | 页面结构、交互、验收           |
| `02-API与表结构设计.md`     | 接口、表、缓存、复盘规则         |
| `03-复盘规则测试用例.md`      | 无遥测标签判定样例            |
| `04-Telemetry解析说明.md` | 事件映射、采样、本地存储         |
| `05-API提额申请材料.md`     | 提额中英对照材料（缓存/请求模式/合规） |

---

## 8. 变更记录

| 日期         | 变更                                                           |
|------------|--------------------------------------------------------------|
| 2026-08-27 | 初版：汇总 PRD/API 里程碑与筹备待办                                       |
| 2026-08-28 | 对齐 M1 完成项；更新剩余待办与执行顺序                                        |
| 2026-08-28 | M1 收尾：refresh 冷却、seasons 下拉、错误态与合规标注                         |
| 2026-08-28 | M2 核心：无遥测复盘引擎、report API、对局报告卡、列表标签列、用例文档                    |
| 2026-08-28 | M2 增强：player analysis 聚合、weaknessTags、分析 Tab、localStorage 收藏 |
| 2026-08-28 | M3：telemetry 管道、events/parse API、事件轴、2D 回放 MVP、文档 04         |
| 2026-08-28 | M4 务实版：本地历史库、同步近况、武器/地图/对比、CSV、改名提示                          |
| 2026-08-28 | 遥测增强报告：`report-engine-telemetry`、ready 自动重算、用例补充             |
| 2026-08-28 | trend 近14天、网关 IP 限流（proxy）、api_sync_log JSONL                |
| 2026-08-28 | 分享卡 `/share/match` + 复制链接；parser 1.1.0 枪线图层                  |
| 2026-08-28 | 落盘：`.data/matches                                            |reports|seasons`；提额材料 `05-API提额申请材料.md` |
| 2026-08-28 | 车队同场统计 P0：insights、CSV、`.data/squad` 缓存（refresh=1）           |
| 2026-08-28 | 玩家页近况现状卡：FormStatusCard + 概览并行加载 form/anomalies              |
