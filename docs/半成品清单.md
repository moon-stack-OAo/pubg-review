# 半成品清单与下一刀

> 更新日期：2026-09-01  
> 目的：把「看起来有、其实不闭环」的能力收成可执行 backlog。  
> 原则：未 PUSH 前视为同一版本变动；**不提前改版本号**。  
> 相关：`docs/00-待办清单.md`、`docs/01-PRD-页面线框.md`、`docs/06-缺口补充-AI与地图.md`、`CHANGELOG.md`

## 状态图例

| 标记    | 含义  |
|-------|-----|
| `[ ]` | 未开始 |
| `[~]` | 进行中 |
| `[x]` | 已完成 |
| `[-]` | 暂缓  |

## 成熟度总览

| 模块             | 成熟度  | 一句话                     |
|----------------|------|-------------------------|
| 搜索 / 玩家概览      | ✅    | 主路径可用，对局深挖弱             |
| 近况 form / 弱点分析 | 🟡   | 有卡有标签，诊断可信度一般           |
| 对局复盘报告         | 🟡   | 可读，增强依赖 telemetry ready |
| 事件轴 / 2D 回放    | 🟡   | 自标 MVP，难「验报告」           |
| 武器 / 地图 Tab    | 🟡   | 常半空，依赖历史 + 已 parse      |
| 对比             | 🟡   | 能比 KPI，难比打法             |
| 车队             | ✅～🟡 | 有价值，齐全场样本易空             |
| 本地历史库          | 🟡   | 本机 OK，自动留存断             |
| Remote MCP     | ✅～🟡 | 13 Tools + Token，公网需运维  |
| 部署 / 生产化       | 🔴   | 可自托管，不可水平扩展             |

**总判**：M1–M4 务实版功能面已铺开；真正拉开价值的三块——**自动留存、可验证回放、可信分析**——仍明显半成品。

---

## P0 — 用户价值最大（建议按序）

### P0-1 收藏 → 历史库留存闭环

- 现状：收藏页已支持单人/全部串行同步；文案已去掉「后续里程碑」
- 证据：`src/app/favorites/page.tsx`、`src/lib/errors.ts`、`docs/01` §5.2
- 验收：收藏页可「同步全部」；串行 + 全局冷却；失败可重试；不同步官方全量滥打

```text
[x] 收藏页「同步近况」单人入口复用现有 sync API
[x] 收藏页「同步全部」串行队列（90s 级全局冷却 / 可取消）
[x] 进度 UI（成功/跳过/失败原因）
[x] 修正 errors / 收藏页文案，去掉「后续才有」误导
[ ] （可选）上次同步时间展示
```

### P0-2 事件轴 ↔ 2D 回放联动

- 现状：轴与回放割裂；无 `?t=` 跳转；回放自标 MVP
- 证据：`match-timeline.tsx`、`match-replay.tsx`、PRD `tab=replay&t=`
- 验收：点事件进入回放对应时刻；分享/刷新可恢复 `t`

```text
[x] 事件轴点击 → `/match/...?tab=replay&t=秒`
[x] 回放读取 URL `t` 并定位
[x] 回放内当前事件高亮（可选）
[x] 队伍/自己轨迹默认跟随（最小可用）
```

### P0-3 分析可信度（弱样本不装懂）

- 现状：雷达粗算、默认 50；文案与遥测增强现状不一致
- 证据：`player-analysis-core.ts`、玩家页 AnalysisSection、README 遥测描述
- 验收：弱样本明确降级；有 telemetry 报告优先；文档与 `1.1.0-telemetry` 对齐

```text
[x] 展示 sampleSize / 置信度（或「样本不足」态）
[x] 弱样本：隐藏假雷达或改为「暂无法判断」
[x] 聚合经 `buildMatchReport` 已自动选用增强/降级报告（UI 文案已对齐）
[x] README / docs/04 对齐「降级初判 vs 遥测增强」真实行为
```

---

## P1 — 体验明显半空

### P1-1 武器 Tab 半空治理

```text
[x] 同步近况时可选「顺带 parse 最近 N 场」（默认关，限流友好）
[x] 武器 Tab 展示「已 parse / 未 parse」进度与引导 CTA
[ ] （可选）telemetry 武器补伤害占比等字段（有事件才启用）
```

### P1-2 地图 Tab 可点进

```text
[x] 地图卡点击 → 过滤该地图的对局列表（query 或锚点）
[x] 对外文案标明 KD≈ 为近似，避免误解
```

### P1-3 对局页 / 玩家页 Tab 补齐（相对 PRD）

```text
[x] 对局页 `tab=weapons`（本场，telemetry ready 才启用）
[x] 玩家页对局区升级：筛选 / 排序 / 简单分页（或独立 matches Tab）
```

### P1-4 对比增强

```text
[x] 近况 / 弱点并排（不止赛季 KPI）
[x] 好友/最近搜索快捷填入 `vs=`
[ ] （暂缓）第三人、同段位参考
```

---

## P2 — 生产化与传播

### P2-1 单机生产化（不做 SaaS 也可）

```text
[x] `.data/` 备份脚本说明 + 示例（deploy/backup-data.sh.example；cron / 保留 N 天 / 恢复）+ 磁盘占用巡检（deploy/README）
[x] 明确对外 SLA / 已知限制：单实例、无 Redis、进程内限流与冷却、重启丢内存态、持久靠 `.data`、禁 PM2 cluster
[ ] Demo 公网 URL（提额材料勾选；替换占位后方可勾，勿伪造）
[ ] developer.pubg.com 提额正式提交（材料已备；须人工，仓库无法代勾）
[-] Redis：限流 / 缓存 / 多实例 — 暂缓；当前单机 `.data` + 进程内限流足够，引入需改 async 限流接口且无多实例需求
```

### P2-2 分享闭环

```text
[x] OG / 分享海报最小图（排名、击杀、主因标签）
[x] 分享卡与报告增强态一致（避免「有增强但分享很空」）
```

### P2-3 车队稳健性

```text
[ ] 无齐全场时的明确空态与「去同步/降 limit」引导
[ ] （暂缓）跨设备车队档案
```

### P2-4 工程债（影响协作）

```text
[ ] 评估是否取消 gitignore `/docs/`（或改为发布精简 docs）
[ ] telemetry parse 队列化（防并发顶满）
[ ] PRD §9 验收清单与 `00` 里程碑勾选对齐
```

---

## 建议落地顺序（2～3 个迭代）

| 迭代    | 主题    | 交付                                | 状态    |
|-------|-------|-----------------------------------|-------|
| **A** | 闭环与可信 | P0-1 收藏批量同步 + P0-3 文档/弱样本降级       | ✅ 已落地 |
| **B** | 可验证复盘 | P0-2 轴↔回放 `t=` + P1-1 武器 parse 引导 | ✅ 已落地 |
| **C** | 体验补齐  | P1-2 地图点进 + P1-3 本场武器 + P2-2 分享海报 | ✅ 已落地 |

---

## 下一专题（规划）

AI 可选增强、官方 api-assets 底图、载具/船图层、建筑 POI 近似：见 **`docs/06-缺口补充-AI与地图.md`**（迭代 1～4）。  
说明：原「官方地图贴图级回放」暂缓项改为「可用 api-assets，按 06 文档推进」；逐房间密室 / 非官方贴图来源仍不做。

## 明确暂缓（先不做）

```text
[-] 完整 OAuth / 多租户账号体系
[-] MySQL 正式库替换 `.data/`（除非要上多实例）
[-] 非 api-assets 来源的「官方风」贴图 / 私服解包图
[-] 逐房间密室 / 完整建筑碰撞
[-] 移动端专项适配
[-] 四平台同等验收（先保证 steam 质量）
```

---

## 快速对照：什么叫「做完」

| 能力 | 半成品表现    | 做完表现                   |
|----|----------|------------------------|
| 收藏 | 只存昵称     | 一键把收藏打进历史库             |
| 回放 | 能拖进度条    | 从死亡/击杀事件一键跳到那一秒        |
| 分析 | 总有雷达分    | 样本不够就直说不够              |
| 武器 | 经常暂无明细   | 知道还差几场 parse，并能补       |
| 部署 | 能 PM2 起来 | 备份、限流、MCP Token、提额路径清晰 |
