# 07 — 功能缺口总览

> 更新日期：2026-09-17  
> 目的：汇总「已铺开但未闭环 / 规划未开工 / 明确暂缓」三类缺口，便于排期。  
> 相关：`docs/半成品清单.md`、`docs/06-缺口补充-AI与地图.md`、`docs/00-待办清单.md`、`README.md`、`CHANGELOG.md`  
> 原则：未 PUSH 前视为同一版本变动；**不提前改版本号**。

## 1. 现状总判

| 层级        | 结论                                                 |
|-----------|----------------------------------------------------|
| M1–M4 务实版 | 主路径已可用（搜索 → 概览 → 报告 → 遥测回放 → 历史/武器/地图/对比/车队）       |
| 半成品收口 A–C | 收藏批量同步、轴↔回放 `t=`、弱样本降级、武器 parse 引导、地图点进、分享 OG 等已落地 |
| 真正拉开价值的三块 | **自动留存深度、可验证回放（底图/图层）、可信分析（规则+可选 AI）** 仍缺口最大       |
| 生产形态      | 单实例 Node + `.data/`；可自托管，不可水平扩展                    |

图例：`[ ]` 未开始 · `[~]` 进行中 · `[x]` 已完成 · `[-]` 暂缓 · `(可选)` 非阻塞

---

## 2. 缺口矩阵（按优先级）

### P0 — 用户价值 / 生产阻塞

| ID   | 缺口            | 现状            | 建议验收                       | 出处              |
|------|---------------|---------------|----------------------------|-----------------|
| G-01 | Demo 公网 URL   | 提额材料仍为占位      | 可公网访问后替换 `05` 占位再勾选        | `半成品` P2-1、`05` |
| G-02 | 官方 API 提额正式提交 | 材料已备；仓库无法代勾   | 人工登录 developer.pubg.com 提交 | `00` §4、`05`    |
| G-03 | 车队无齐全场空态引导    | 样本易空，引导弱      | 明确空态 +「去同步 / 降 limit」      | `半成品` P2-3      |
| G-04 | （可选）收藏上次同步时间  | 可同步，无「上次成功」展示 | 收藏卡 / 玩家页展示最近 syncAt       | `半成品` P0-1      |

### P1 — 体验半空 / 可信度

| ID   | 缺口          | 现状                          | 建议验收                    | 出处         |
|------|-------------|-----------------------------|-------------------------|------------|
| G-10 | 武器伤害占比等遥测字段 | 有 parse 进度，明细仍偏 participant | 有 events 时补伤害占比等        | `半成品` P1-1 |
| G-11 | 回放「能验报告」不足  | 有轨迹/枪线/`t=`，无官方底图与载具层       | 见专题 `06` 迭代 1～2         | `06` A/B   |
| G-12 | 分析诊断深度一般    | 规则+模板；弱样本已降级                | 规则补强（载具/POI/空投）± 可选 LLM | `06` D/E   |
| G-13 | 对比难比打法      | 仅 KPI + 近况/弱点并排             | 暂不扩第三人；可先比同场报告标签        | `半成品` P1-4 |

### P2 — 工程债 / 运维 / 传播

| ID   | 缺口                  | 现状                      | 建议验收                                       | 出处                 |
|------|---------------------|-------------------------|--------------------------------------------|--------------------|
| G-20 | telemetry parse 队列化 | 并发易顶满官方 RPM             | 串行/队列 + 冷却，禁定时全量扫库                         | `半成品` P2-4、`06` F4 |
| G-21 | PRD §9 与 `00` 里程碑对齐 | 文档可能漂移                  | 验收清单与勾选一致                                  | `半成品` P2-4         |
| G-22 | docs 发布策略           | 本地 `docs/` 存在；是否进仓库需约定  | 评估 gitignore / 发布精简 docs                   | `半成品` P2-4         |
| G-23 | `.data` 膨胀治理产品化     | 仅有 deploy 巡检说明          | 可选清理策略 UI 或脚本固化                            | `deploy/README`    |
| G-24 | MCP 缺遥测验回放 Tools    | 13 Tools，无 parse/events | `parse_telemetry` / `get_telemetry_events` | `06` E4            |

### 已落地（本缺口表不再跟踪）

```text
[x] 多 API Key：PUBG_API_KEYS 轮询，遇 429 切换（client.ts）
[x] Remote MCP 默认关闭 + Bearer
[x] PM2 / Nginx / 备份示例 / 单机 SLA 说明
[x] 收藏单人/全部串行同步；轴↔回放 t=；弱样本降级；分享 OG
```

---

## 3. 下一专题缺口（未开工）

完整条目见 **`docs/06-缺口补充-AI与地图.md`**。此处只列标题级：

| 专题            | 缺口摘要                                     | 推荐迭代 |
|---------------|------------------------------------------|------|
| A 官方地图底图      | mapName 映射、Low_Res 资源、坐标抽检、署名            | 1    |
| B 载具/船/空投/死亡点 | LogVehicle* 解析、回放图层、空投/死亡标记、队伍面板         | 1～2  |
| C 建筑/密室近似     | 预期管理 + POI；不做逐房间                         | 3    |
| D 可选 LLM      | 规则仍是事实源；默认关；脱敏 JSON；失败降级                 | 4    |
| E 规则补强        | vehicle_risk / POI / 空投标签；MCP 遥测 Tools   | 2～3  |
| F 工程合规        | LICENSE 清单、parserVersion、回放性能、与 parse 串行 | 贯穿   |

---

## 4. 明确暂缓（先不做）

```text
[-] 完整 OAuth / 多租户账号体系
[-] MySQL 正式库替换 `.data/`（除非要上多实例）
[-] Redis 限流/缓存/多实例（当前单机足够）
[-] 非 api-assets 来源的「官方风」贴图 / 私服解包图
[-] 逐房间密室 / 完整建筑 mesh / 门窗碰撞
[-] 移动端专项适配
[-] 四平台同等验收（优先保证 steam）
[-] 收藏玩家定时同步 Job（改用手动「同步近况」）
[-] 跨设备车队档案
[-] 对比第三人、同段位参考
[-] 用 AI「判定开挂」（banType 仅官方封禁状态）
[-] 武器精通 Mastery（增值后续）
```

---

## 5. 建议排期（2～3 个迭代）

| 迭代    | 主题     | 交付                               | 依赖               |
|-------|--------|----------------------------------|------------------|
| **D** | 上线与提额  | G-01 公网 Demo → G-02 提额；G-03 车队空态 | 一台可访问机器          |
| **E** | 可验证回放  | `06` 迭代 1（底图 + 空投/死亡点）           | api-assets、体积策略  |
| **F** | 图层与规则  | `06` 迭代 2～3（载具 + POI + E1/E2）    | parserVersion 升级 |
| **G** | （可选）AI | `06` 迭代 4；无 Key 行为不变             | 网关 Key、限流        |

半成品 A～C 已完成，不再占用主排期；残余 `(可选)` 项可穿插在 D/E。

---

## 6. 「做完」对照

| 能力        | 缺口表现           | 做完表现                     |
|-----------|----------------|--------------------------|
| 提额 / Demo | 材料占位、未提交       | 公网 Demo 可点；developer 已提交 |
| 回放        | 纯色画布 + 轨迹      | 官方底图 + 关键点 + 可开关载具层      |
| 分析        | 模板文案、诊断一般      | 规则标签更贴战局；可选 AI 可关可降级     |
| 车队        | 无齐全场时懵         | 空态说清原因与下一步               |
| 遥测工程      | 并发 parse 易 429 | 队列化；多 Key 轮询兜底           |
| 部署        | 能起来            | 备份/巡检/提额路径清晰（已大部分具备）     |

---

## 7. 文档索引

| 文件                 | 角色                |
|--------------------|-------------------|
| `07-功能缺口.md`       | **本文**：缺口总览与排期入口  |
| `半成品清单.md`         | 半成品闭环明细（A～C 多已勾完） |
| `06-缺口补充-AI与地图.md` | AI / 地图 / 载具专题规格  |
| `00-待办清单.md`       | 里程碑历史与勾选          |
| `05-API提额申请材料.md`  | 提额材料（勿伪造已公网/已提额）  |
| `deploy/README.md` | 单机部署、备份、SLA       |

---

## 8. 变更记录

| 日期         | 变更                              |
|------------|---------------------------------|
| 2026-09-17 | 初版：汇总半成品残余、`06` 专题、暂缓项与建议迭代 D～G |
